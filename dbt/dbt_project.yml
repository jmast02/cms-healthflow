name: cms_healthflow
version: '1.0.0'
config-version: 2

profile: cms_healthflow

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  cms_healthflow:
    staging:
      +schema: silver
      +materialized: view
    marts:
      +schema: gold
      +materialized: table
