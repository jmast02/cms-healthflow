# Spark job container dependencies — requires Java (see Dockerfile.spark)
python-dotenv==1.0.1
requests==2.31.0
tqdm==4.66.4

# Big data — plain Parquet (snappy compressed), no Delta Lake JARs needed
pyspark==3.5.1
pyarrow==16.1.0
pandas==2.2.2

# Testing
pytest==8.2.2
pytest-asyncio==0.23.7
httpx==0.27.0

# Storage
psycopg2-binary==2.9.9
minio==7.2.7
boto3==1.34.131

# Data quality
great-expectations==0.18.16
